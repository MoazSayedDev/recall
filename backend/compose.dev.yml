services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev

    ports:
      - '8000:8000'

    env_file:
      - .env

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run start:dev